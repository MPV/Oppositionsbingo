<h1>Nytt påstående</h1>

<% form_for(@claim) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => { :f => f } %>
  
  <p>
    <%= f.submit 'Skapa' %>
  </p>
<% end %>

&larr; <%= link_to 'Tillbaka', claims_path %>