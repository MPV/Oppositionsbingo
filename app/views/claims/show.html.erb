<p>
  <b>Själva påståendet:</b>
  <%=h @claim.name %>
</p>

<% if @claim.used_squares.length > 0 %>
  
  <h4>Hänt enligt:</h4>
  
  <ul>
  <% for square in @claim.used_squares %>
    <li>
      <% if square.card.nil? %>
        Bingobrickan saknas.
      <% else %>
        <%= link_to square.card.owner, square.card %>
      <% end %>
    </li>
  <% end %>
  </ul>

<% else %>

  <h4>Ingen har markerat detta påstående som uppfyllt än.</h4>

<% end %>

<%= link_to 'Ändra', edit_claim_path(@claim) %> |
<%= link_to 'Tillbaka', claims_path %>