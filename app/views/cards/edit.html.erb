<h1>Ändrar bingobricka</h1>

<% form_for(@card) do |f| %>
  <%= f.error_messages %>
  
  <%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit 'Uppdatera' %>
  </p>
  
<% end %>

&larr; <%= link_to 'Tillbaka', cards_path %> eller <%= link_to 'Visa', @card %>