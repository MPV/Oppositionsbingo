<h1>Ändrar bingobricka</h1>

<% form_for(@card) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :owner, 'Namn' %><br />
    <%= f.text_field :owner %>
  </p>
  <p>
    <%= f.submit 'Uppdatera' %>
  </p>
<% end %>

<%= link_to 'Visa', @card %> |
<%= link_to 'Tillbaka', cards_path %>